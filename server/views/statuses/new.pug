extends ../layout

block content
  h1= t('createStatus')
  form(action="/statuses" method="POST")
    .mb-3
      label.form-label(for="name") #{t('name')}
      input.form-control(type="text" name="name" id="name")
    button.btn.btn-primary(type="submit") #{t('createStatus')}